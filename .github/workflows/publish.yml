name: Publish

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Package registry publication is disabled until the Rust-engine package set and release order are finalized."
