name: Publish

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "npm publishing is handled by the CI workflow's publish-npm job after the release job creates a tag."
